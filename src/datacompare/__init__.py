"""datacompare - 前后数据集全字段对比工具。

核心能力：
  * 支持 CSV / 自定义分隔符文本 / Excel / Parquet / JSON 作为输入
  * 全量数据落 DuckDB（大文件也能跑，走磁盘而非内存）
  * 逐字段对比，区分「真正数值不同」和「仅格式不同（补零/千分位/大小写/空白）」
  * 输出 HTML / Excel / Markdown / 控制台 报告
  * 自动识别重复主键、空值率突变、数值离群点等异常数据
"""

__version__ = "0.2.0"

__all__ = ["__version__"]
